{
  "name": "kzhigalov-portfolio",
  "private": true,
  "scripts": {
    "build:experience": "esbuild experience.js --bundle --minify --format=iife --target=es2020 --outfile=assets/experience.bundle.js"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "three": "0.186.1"
  }
}
